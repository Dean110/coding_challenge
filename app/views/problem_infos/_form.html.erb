<%= form_with(model: problem_info, local: true) do |form| %>
  <% if problem_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(problem_info.errors.count, "error") %> prohibited this problem_info from being saved:</h2>

      <ul>
        <% problem_info.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer %>
  </div>

  <div class="field">
    <%= form.label :username_id %>
    <%= form.text_field :username_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
